# Beneloil — browser build

This repository carries only the exported Godot web build so GitHub Pages can
serve it. It is rewritten as a single commit on every publish; the source
lives in the private `Benerits/Beneloil-godot` repository and the build is
pushed from there by `tools/publish_web.sh --upload`.
